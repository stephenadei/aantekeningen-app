# Suppress deprecation warnings
audit-level=moderate
fund=false

# Shared @stephen/* packages come from the host-local Verdaccio registry
# (read is open on localhost; no token needed to install).
@stephen:registry=http://127.0.0.1:4873/
legacy-peer-deps=true
