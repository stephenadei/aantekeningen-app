# Suppress deprecation warnings
audit-level=moderate
fund=false
legacy-peer-deps=true

# Shared @stephenadei/* packages come from GitHub Packages.
# Auth: locally via ~/.npmrc; in CI via actions/setup-node (NODE_AUTH_TOKEN).
@stephenadei:registry=https://npm.pkg.github.com
